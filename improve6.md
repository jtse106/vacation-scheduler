to do list -- put x next to each when done:

When you click on a individual day, there's a pop up. And then there's names for each person. However when you click the edit button nothing happens. I'm not sure what this is supposed to do. Either fix it or get rid of it. And it probably shouldn't be an edit button for the user view unless it's pertaining to the actual user who is logged in. You shouldn't see an edit button that doesn't do anything if you're a user and it's appearing on other doctors 'cause you wouldn't be allowed to edit them anyway. 

look at snapshot Screenshot 2026-04-04 130721:  The Brown circle shows dates that don't have any DR signed up. For some reason all of the lines have space together really closely and it looks terrible. They should be spaced the same way as on dates that have DR signed up. But then if you look at dates that have DR signed up, there's still some wasted space at the bottom. The names are not evenly spaced among open Date. So look at the brown arrow that's pointing up and down at the bottom of the day that shows doctors that are filling up the slots , and you'll see that there's empty space. The doctors that are filling up the slots should be evenly spaced so that this brown arrow section doesn't really exist, and the doctors are taking up the majority of the box so it looks good and there's no unreasonable wasted space.

The admin console text goes way to the left side of the card. It looks super ugly. There needs to be some padding. This is in reference to each subsection like Scheduled vacation requests, users, holidays, vl export, etc.

the settings button Opens a new Pop up that displays the skin's color changer as well as password changer. This is great however the size of this pop up window should remain constant when switching between these two options, the skin versus the password changer. Also if you look at this snapshot Screenshot 2026-04-04 131223 Look at the highlighted blue scroll bars. This shows that size of the pop up is incorrect and it's needing scroll bars. We shouldn't have scroll bars if it is sized correctly. So please size it correctly. And then it should not resize when change the options. This size should remain constant otherwise the resize is jerky and not a pleasant user experience. 

The improvement with the eyes icons for the Password is great, however you don't need a little box around the eye.

Make the top banner go all the way across the whole top. Make the two left pane cards total vertical length equal the same size as the vertical length of the calendar so it matches at the bottom.

no need for a settings or login button at the top right or the "SB South Bay ED VL Schedule" at the top left on the login screen

When you click on a date while logged in as a user, It should select the date. You should be able to click and drag and select multiple dates. Once the dates are selected you should be able to press the enter or click a button that allows you to assign yourself to those shifts. The same thing should apply for deleting yourself off a shift, by pressing the backspace or delete button when shifts are select where there should also be a delete button. I don't think that the functionality of the window is really that helpful when you click on a date. Especially on the user side. You can however keep pop up window for the admin side of things.

When a user asks to add a different user that they don't have permissions for to add a shift, It should just say something like they don't have permission to add this doctor because they're not delegated appropriately or something like that. Instead, right now it ends up adding that doctor to the wait list which is definitely wrong. Any user shouldn't be able to control other users. for example somehow i added amezquita from april 25-27 while logged in as afifi.

when adding a shift via the 'ask the assistant', when you press return it should act as if it's pressing the ok button. and make the button say add vacation instead of parse and schedule.

no need to show the select physician on the Schedule vacation popup for users, unless the use is delegated to control multiple physicians

remove the dictation feature. it doesn't seem to work. -- unless you can get it to work.

I added the credentials for Gmail api In the .env file. So please use this For all features that need to send emails in this web application.

Whenever you are adding a new user, Instead adding a password for them, instead send them an email with a link on it to a password reset form that's specific to that user. You can send the email via the Gmail api. This should be the default option. There should be an option in admin console that allows for assigning a password when creating a new user instead -- but this shouldn't be the main option. This could be set by admin, or Have a randomly generate button as well. Same thing applies where you should be able to have that little eye that shows the characters or hides the characters. This should all be standard website stuff.



 
to do list. read in entirety first. place X next to each one whe complete.


X I didn't get the email invite when i made a new user... and it still shows the default as adding a password. there should be a Check box or something that says manually set the password instead of  Adding link to set the initial password for the newly created user. And I didn't see a random generation button for password if someone chooses to manually set the password. make this update.

X the bottom slots are just a hair too close to the bottom of each day. can you move them slightly up? there should still be equal spacing for the slots vertically within each day, it just needs to be not so close to the bottom. just a little bit higher should be good. see screenshot red circle for further details Screenshot 2026-04-04 151638.

X the update password card is crazy big, doesn't need to fill the entire popup. i like that the popup size matches the popup size for the skins in the settings, but the update password doesn't need to fill the whole thing. see screenshot red circle Screenshot 2026-04-04 151846

X when logging in, there needs to be a forgot password? button that opens a popup where you enter your email and it sends an email with link to reset password for that user.

X when i check my google gmail cloud api, i don't seen any activity which makes me think no emails have been sent via the api.

X try to fit the calendar to the screen's resolution and window size so it fits well vertically, top to bottom.

X adjust the sub-pages so they look like the main calendar page:
"Request History
Holiday Rotation
Legacy Calendars
Instructions
Guidelines
Admin Console"
